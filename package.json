{
  "name": "tree-sitter-stylus",
  "version": "0.2.0",
  "description": "Stylus (CSS preprocessor) grammar for tree-sitter",
  "license": "MIT",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "stylus",
    "css"
  ],
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "src",
    "queries"
  ],
  "scripts": {
    "sweep": "node scripts/sweep.mjs"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.26.0"
  }
}
